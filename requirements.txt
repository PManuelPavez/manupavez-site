# Dependencias del script de automatización de fechas (SoundCloud)
# La planilla se lee vía Web App de Apps Script (sin gspread ni Service Account).
# Instalar con:  pip install -r requirements.txt

requests>=2.31.0        # Peticiones HTTP (Web App de Sheets + API de SoundCloud)
python-dotenv>=1.0.0    # Carga de variables de entorno desde el archivo .env
